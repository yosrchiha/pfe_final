/**
 * Mode personnel administrateur.
 * L'exploration utilise le même formulaire que le client :
 * l'administrateur fournit son token GitLab et explore ses propres projets.
 * Le formulaire redirige ensuite vers /explorer comme dans l'espace client.
 */
export { default } from "../../Exploreformpage/page";
